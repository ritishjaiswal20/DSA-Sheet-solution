#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
   vector<int> v;
   v= {1,3,4,2,3};
   sort(v.begin(),v.end());
   for(int i=1;i<v.size();i++)
   {
       if(v[i]==v[i-1])
       {
           cout<<v[i];
       }
   }

    return 0;
}
